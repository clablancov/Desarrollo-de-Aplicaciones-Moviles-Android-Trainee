<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.primeraapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PrimeraApp">
        <activity android:name=".opSpinner"></activity>
        <activity android:name=".opswitch" />
        <activity android:name=".OPCheckBox" />
        <activity android:name=".OPRadioButton" />
        <activity android:name=".Dividir" />
        <activity android:name=".Multiplicar" />
        <activity android:name=".Restar" />
        <activity android:name=".Sumar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>